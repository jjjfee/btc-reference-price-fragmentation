Tested environment snapshot
===========================

Test date: 2026-07-23
Operating system: Windows-11-10.0.26200-SP0
Python: 3.12.13
pip: 26.0.1

Purpose and limits
------------------

This is the exact environment used for the repository-level validation and
synthetic reviewer smoke checks listed below. It is an observed Windows
snapshot, not a cross-platform lock file and not a guarantee that every
permitted version combination in requirements.txt behaves identically.

The package list was captured with:

    python -m pip freeze --exclude artifact-tool-v2

The excluded package is a bundled workspace-only artifact utility installed
from a local build path; it is not imported by this repository. No scientific
dependency was removed from the snapshot.

Package snapshot
----------------

annotated-types==0.7.0
cffi==2.1.0
charset-normalizer==3.4.9
colorama==0.4.6
contourpy==1.3.3
cryptography==49.0.0
cycler==0.12.1
et_xmlfile==2.0.0
fonttools==4.63.0
kiwisolver==1.5.0
llvmlite==0.48.0
lxml==6.0.2
matplotlib==3.11.1
mpmath==1.4.1
numba==0.66.0
numpy==2.3.5
openpyxl==3.1.5
packaging==26.2
pandas==3.0.1
pdf2image==1.17.0
pdfminer.six==20251230
pdfplumber==0.11.9
pillow==12.2.0
pyarrow==25.0.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pyhumps==3.8.0
pyparsing==3.3.2
pypdf==6.10.0
pypdfium2==5.11.0
python-dateutil==2.9.0.post0
python-docx==1.2.0
python-pptx==1.0.2
reportlab==4.4.9
scipy==1.18.0
setuptools==83.0.0
six==1.17.0
tqdm==4.69.0
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.2
wheel==0.47.0
xlsxwriter==3.2.9

Passed commands
---------------

    python -m compileall -q src scripts archive tests
    python scripts/final/validate_selected_outputs.py
    python -m unittest discover -s tests -p "test_*.py" -v

All nine documented entry-point help commands in REPLICATION.md, including
the fixed-composition checker and local hash utility, exited successfully.

The no-data fixed-composition command stopped before creating output with the
expected generated-input diagnostic. An isolated headline-only run using the
retained local inputs reproduced all four exact headline values with zero
absolute error, 100,000 base minutes, 800,000 events, and no event-key
mismatches. It did not run the later fixed-composition or bootstrap stages.
